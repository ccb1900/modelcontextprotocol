---  
标题: "构建MCP服务器"  
描述: "开始构建你自己的服务器，以便在桌面版Claude及其他客户端中使用。"  

在本教程中，我们将构建一个简单的MCP天气服务器，并将其连接到主机（桌面版Claude）。我们将从基础设置开始，逐步过渡到更复杂的应用场景。  

### 我们将构建什么  

目前许多大语言模型尚不具备获取天气预报和极端天气警报的能力。让我们用MCP来解决这个问题！  

我们将构建一个提供两种工具的服务器：`get_alerts`（获取警报）和`get_forecast`（获取预报）。然后将其连接到MCP主机（本例中使用桌面版Claude）：  

<Frame>  
  <img src="/images/weather-alerts.png" />  
</Frame>  
<Frame>  
  <img src="/images/current-weather.png" />  
</Frame>  

<Note>  

服务器可以连接任何客户端。这里选择桌面版Claude是为了简化演示，但我们还提供了[构建自定义客户端指南](/quickstart/client)以及[其他客户端列表](/clients)。  

</Note>  

### MCP核心概念  

MCP服务器可提供三种主要能力：  

1. **资源**：客户端可读取的类文件数据（如API响应或文件内容）  
2. **工具**：大语言模型可调用的函数（需经用户批准）  
3. **提示**：帮助用户完成特定任务的预制模板  

本教程将主要聚焦工具的实现。  

<Tabs>  
<Tab title='Python'>  

让我们开始构建天气服务器！[完整代码可在此处获取](https://github.com/modelcontextprotocol/quickstart-resources/tree/main/weather-server-python)  

### 预备知识  

本快速入门指南假定您已熟悉：  

- Python  
- Claude等大语言模型  

### MCP服务器日志记录  

实现MCP服务器时，需特别注意日志处理方式：  

**对于基于STDIO的服务器：** 切勿写入标准输出(stdout)，包括：  

- Python中的`print()`语句  
- JavaScript中的`console.log()`  
- Go中的`fmt.Println()`  
- 其他语言中类似的stdout函数  

写入stdout会破坏JSON-RPC消息格式导致服务器故障。  

**对于基于HTTP的服务器：** 标准输出日志记录没有问题，因其不会干扰HTTP响应。  

### 最佳实践  

1. 使用写入stderr或文件的日志库  

### 简单示例  

```python  
# ❌ 错误做法(STDIO)  
print("正在处理请求")  

# ✅ 正确做法(STDIO)  
import logging  
logging.info("正在处理请求")  
```  

### 系统要求  

- 已安装Python 3.10或更高版本  
- 必须使用Python MCP SDK 1.2.0或更高版本  

### 环境设置  

首先安装`uv`并设置Python项目环境：  

<CodeGroup>  

```bash macOS/Linux  
curl -LsSf https://astral.sh/uv/install.sh | sh  
```  

```powershell Windows  
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"  
```  

</CodeGroup>  

操作完成后请重启终端以确保识别`uv`命令。  

现在创建并设置项目：  

<CodeGroup>  

```bash macOS/Linux  
# 创建项目目录  
uv init weather  
cd weather  

# 创建虚拟环境并激活  
uv venv  
source .venv/bin/activate  

# 安装依赖项  
uv add "mcp[cli]" httpx  

# 创建服务器文件  
touch weather.py  
```  

```powershell Windows  
# 创建项目目录  
uv init weather  
cd weather  

# 创建虚拟环境并激活  
uv venv  
.venv\Scripts\activate  

# 安装依赖项  
uv add mcp[cli] httpx  

# 创建服务器文件  
new-item weather.py  
```  

</CodeGroup>  

现在让我们开始构建服务器。  

## 构建服务器  

### 导入包并设置实例  

在`weather.py`文件顶部添加：  

```python  
from typing import Any  
import httpx  
from mcp.server.fastmcp import FastMCP  

# 初始化FastMCP服务器  
mcp = FastMCP("weather")
# 常量定义  
NWS_API_BASE = "https://api.weather.gov"  
USER_AGENT = "weather-app/1.0"  
```  

FastMCP类利用Python类型提示和文档字符串自动生成工具定义，使得创建和维护MCP工具变得简单高效。  

### 辅助函数  

接下来，我们添加用于查询和格式化美国国家气象局API数据的辅助函数：  

```python  
async def make_nws_request(url: str) -> dict[str, Any] | None:  
    """向NWS API发起请求并妥善处理错误。"""  
    headers = {  
        "User-Agent": USER_AGENT,  
        "Accept": "application/geo+json"  
    }  
    async with httpx.AsyncClient() as client:  
        try:  
            response = await client.get(url, headers=headers, timeout=30.0)  
            response.raise_for_status()  
            return response.json()  
        except Exception:  
            return None  

def format_alert(feature: dict) -> str:  
    """将警报特征格式化为易读字符串。"""  
    props = feature["properties"]  
    return f"""  
事件: {props.get('event', '未知')}  
区域: {props.get('areaDesc', '未知')}  
严重程度: {props.get('severity', '未知')}  
描述: {props.get('description', '暂无描述信息')}  
应对指南: {props.get('instruction', '未提供具体指导方案')}  
"""  
```  

### 实现工具执行逻辑  

工具执行处理器负责实际执行每个工具的核心逻辑：  

```python  
@mcp.tool()  
async def get_alerts(state: str) -> str:  
    """获取美国各州天气警报。  

    参数:  
        state: 两字母州代码(如CA, NY)  
    """  
    url = f"{NWS_API_BASE}/alerts/active/area/{state}"  
    data = await make_nws_request(url)  

    if not data or "features" not in data:  
        return "无法获取警报信息或未发现警报。"  

    if not data["features"]:  
        return "该州暂无活跃警报。"  

    alerts = [format_alert(feature) for feature in data["features"]]  
    return "\n---\n".join(alerts)  

@mcp.tool()  
async def get_forecast(latitude: float, longitude: float) -> str:  
    """获取指定位置天气预报。  

    参数:  
        latitude: 纬度坐标  
        longitude: 经度坐标  
    """  

    # 首先获取预报网格端点  
    points_url = f"{NWS_API_BASE}/points/{latitude},{longitude}"  
    points_data = await make_nws_request(points_url)  

    if not points_data:  
        return "无法获取该位置的预报数据。"  

    # 从点位响应中提取预报URL  
    forecast_url = points_data["properties"]["forecast"]  
    forecast_data = await make_nws_request(forecast_url)  

    if not forecast_data:  
        return "无法获取详细预报信息。"  

    # 将预报时段格式化为易读文本  
    periods = forecast_data["properties"]["periods"]  
    forecasts = []  
    for period in periods[:5]:  # 仅显示未来5个时段  
        forecast = f"""  
{period['name']}:  
温度: {period['temperature']}°{period['temperatureUnit']}  
风力: {period['windSpeed']} {period['windDirection']}  
预报详情: {period['detailedForecast']}  
"""  
        forecasts.append(forecast)  

    return "\n---\n".join(forecasts)  
```  

### 启动服务  

最后初始化并运行服务：  

```python  
if __name__ == "__main__":  

    # 初始化并运行服务  
    mcp.run(transport='stdio')  
```  

服务部署完成！执行`uv run weather.py`即可启动MCP服务端，该服务将监听来自MCP主机的消息。  

现在我们可以通过现有MCP主机（如桌面版Claude）来测试这个服务端。
## 使用桌面版Claude测试你的服务器

<Note>

桌面版Claude目前暂不支持Linux系统。Linux用户可以继续学习[构建客户端](/quickstart/client)教程，构建一个连接我们刚搭建的服务器的MCP客户端。

</Note>

首先，请确保已安装桌面版Claude。[您可以在此处安装最新版本](https://claude.ai/download)。若已安装，**请务必更新至最新版本**。

我们需要为每个要使用的MCP服务器配置桌面版Claude。为此，请用文本编辑器打开位于`~/Library/Application Support/Claude/claude_desktop_config.json`的配置文件（若文件不存在请新建）。

例如，如果您已安装[VS Code](https://code.visualstudio.com/)：

<CodeGroup>

```bash macOS/Linux
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

```powershell Windows
code $env:AppData\Claude\claude_desktop_config.json
```

</CodeGroup>

然后在`mcpServers`键下添加您的服务器配置。只有当至少配置了一个有效服务器时，桌面版Claude才会显示MCP相关界面元素。

这里我们以单个天气服务器为例：

<CodeGroup>

```json macOS/Linux
{
  "mcpServers": {
    "weather": {
      "command": "uv",
      "args": [
        "--directory",
        "/绝对路径/父文件夹/weather",
        "run",
        "weather.py"
      ]
    }
  }
}
```

```json Windows
{
  "mcpServers": {
    "weather": {
      "command": "uv",
      "args": [
        "--directory",
        "C:\\绝对路径\\父文件夹\\weather",
        "run",
        "weather.py"
      ]
    }
  }
}
```

</CodeGroup>

<Warning>

您可能需要将`command`字段中的`uv`替换为完整路径。在macOS/Linux上可通过`which uv`命令获取路径，Windows上使用`where uv`命令。

</Warning>

<Note>

务必使用绝对路径指定服务器位置。macOS/Linux上可通过`pwd`命令获取路径，Windows命令提示符中使用`cd`命令。在Windows系统中，JSON路径请使用双反斜杠(`\\`)或正斜杠(`/`)。

</Note>

以上配置告知桌面版Claude：

1. 存在名为"weather"的MCP服务器
2. 通过执行`uv --directory /绝对路径/父文件夹/weather run weather.py`命令启动

保存文件后，请重启**桌面版Claude**。

</Tab>

<Tab title='Node'>

让我们开始构建天气服务器！[完整代码可在此处获取](https://github.com/modelcontextprotocol/quickstart-resources/tree/main/weather-server-typescript)

### 预备知识

本快速入门指南假设您已掌握：

- TypeScript
- Claude等大语言模型的基础知识

### MCP服务器日志记录规范

实现MCP服务器时，请特别注意日志处理方式：

**基于STDIO的服务器：** 禁止写入标准输出(stdout)，包括：
- Python中的`print()`语句
- JavaScript中的`console.log()`
- Go中的`fmt.Println()`
- 其他语言的类似stdout函数

写入stdout会破坏JSON-RPC消息格式导致服务器崩溃。

**基于HTTP的服务器：** 标准输出日志不受影响，因为不会干扰HTTP响应。

### 最佳实践

1. 使用写入stderr或文件的日志库，例如Python的`logging`模块
2. JavaScript需特别注意 - `console.log()`默认输出到stdout

### 简单示例

```javascript
// ❌ 错误做法(STDIO)
console.log("服务器已启动");

// ✅ 正确做法(STDIO)
console.error("服务器已启动"); // stderr是安全的
```

### 系统要求

使用TypeScript时，请确保已安装最新版Node.js。
### 设置你的环境

首先，如果你尚未安装 Node.js 和 npm，请先安装它们。你可以从 [nodejs.org](https://nodejs.org/) 下载。  
验证你的 Node.js 安装：

```bash
node --version
npm --version
```

本教程需要 Node.js 16 或更高版本。

接下来，让我们创建并设置项目：

<CodeGroup>

```bash macOS/Linux
# 为项目创建新目录
mkdir weather
cd weather

# 初始化一个新的 npm 项目
npm init -y

# 安装依赖
npm install @modelcontextprotocol/sdk zod
npm install -D @types/node typescript

# 创建项目文件
mkdir src
touch src/index.ts
```

```powershell Windows
# 为项目创建新目录
md weather
cd weather

# 初始化一个新的 npm 项目
npm init -y

# 安装依赖
npm install @modelcontextprotocol/sdk zod
npm install -D @types/node typescript

# 创建项目文件
md src
new-item src\index.ts
```

</CodeGroup>

更新 `package.json`，添加 `"type": "module"` 和一个构建脚本：

```json package.json
{
  "type": "module",
  "bin": {
    "weather": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js"
  },
  "files": ["build"]
}
```

在项目根目录创建 `tsconfig.json`：

```json tsconfig.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "outDir": "./build",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

现在，让我们开始构建你的服务器。

## 构建你的服务器

### 导入包并设置实例

在 `src/index.ts` 顶部添加以下内容：

```typescript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { z } from "zod";

const NWS_API_BASE = "https://api.weather.gov";
const USER_AGENT = "weather-app/1.0";

// 创建服务器实例
const server = new McpServer({
  name: "weather",
  version: "1.0.0",
  capabilities: {
    resources: {},
    tools: {},
  },
});
```

### 辅助函数

接下来，添加用于查询和格式化美国国家气象局（NWS）API 数据的辅助函数：

```typescript
// 用于向 NWS API 发起请求的辅助函数
async function makeNWSRequest<T>(url: string): Promise<T | null> {
  const headers = {
    "User-Agent": USER_AGENT,
    Accept: "application/geo+json",
  };

  try {
    const response = await fetch(url, { headers });
    if (!response.ok) {
      throw new Error(`HTTP 错误！状态码: ${response.status}`);
    }
    return (await response.json()) as T;
  } catch (error) {
    console.error("NWS 请求出错:", error);
    return null;
  }
}

interface AlertFeature {
  properties: {
    event?: string;
    areaDesc?: string;
    severity?: string;
    status?: string;
    headline?: string;
  };
}

// 格式化警报数据
function formatAlert(feature: AlertFeature): string {
  const props = feature.properties;
  return [
    `事件: ${props.event || "未知"}`,
    `地区: ${props.areaDesc || "未知"}`,
    `严重程度: ${props.severity || "未知"}`,
    `状态: ${props.status || "未知"}`,
    `标题: ${props.headline || "无标题"}`,
    "---",
  ].join("\n");
}

interface ForecastPeriod {
  name?: string;
  temperature?: number;
  temperatureUnit?: string;
  windSpeed?: string;
  windDirection?: string;
  shortForecast?: string;
}

interface AlertsResponse {
  features: AlertFeature[];
}

interface PointsResponse {
  properties: {
    forecast?: string;
  };
}

interface ForecastResponse {
  properties: {
    periods: ForecastPeriod[];
  };
}
```
### 实现工具执行功能

工具执行处理器负责实际运行每个工具的逻辑。以下是具体实现：

```typescript
// 注册天气工具
server.tool(
  "get_alerts",
  "获取指定州的天气警报",
  {
    state: z.string().length(2).describe("两字母州代码（如CA、NY）"),
  },
  async ({ state }) => {
    const stateCode = state.toUpperCase();
    const alertsUrl = `${NWS_API_BASE}/alerts?area=${stateCode}`;
    const alertsData = await makeNWSRequest<AlertsResponse>(alertsUrl);

    if (!alertsData) {
      return {
        content: [
          {
            type: "text",
            text: "获取警报数据失败",
          },
        ],
      };
    }

    const features = alertsData.features || [];
    if (features.length === 0) {
      return {
        content: [
          {
            type: "text",
            text: `${stateCode}州暂无活动警报`,
          },
        ],
      };
    }

    const formattedAlerts = features.map(formatAlert);
    const alertsText = `${stateCode}州活动警报：\n\n${formattedAlerts.join("\n")}`;

    return {
      content: [
        {
          type: "text",
          text: alertsText,
        },
      ],
    };
  },
);

server.tool(
  "get_forecast",
  "获取指定位置的天气预报",
  {
    latitude: z.number().min(-90).max(90).describe("位置的纬度"),
    longitude: z
      .number()
      .min(-180)
      .max(180)
      .describe("位置的经度"),
  },
  async ({ latitude, longitude }) => {
    // 获取网格点数据
    const pointsUrl = `${NWS_API_BASE}/points/${latitude.toFixed(4)},${longitude.toFixed(4)}`;
    const pointsData = await makeNWSRequest<PointsResponse>(pointsUrl);

    if (!pointsData) {
      return {
        content: [
          {
            type: "text",
            text: `无法获取坐标${latitude}, ${longitude}的网格点数据（NWS API仅支持美国境内位置）`,
          },
        ],
      };
    }

    const forecastUrl = pointsData.properties?.forecast;
    if (!forecastUrl) {
      return {
        content: [
          {
            type: "text",
            text: "从网格点数据中获取预报URL失败",
          },
        ],
      };
    }

    // 获取预报数据
    const forecastData = await makeNWSRequest<ForecastResponse>(forecastUrl);
    if (!forecastData) {
      return {
        content: [
          {
            type: "text",
            text: "获取预报数据失败",
          },
        ],
      };
    }

    const periods = forecastData.properties?.periods || [];
    if (periods.length === 0) {
      return {
        content: [
          {
            type: "text",
            text: "无可用预报时段",
          },
        ],
      };
    }

    // 格式化预报时段
    const formattedForecast = periods.map((period: ForecastPeriod) =>
      [
        `${period.name || "未知时段"}：`,
        `温度：${period.temperature || "未知"}°${period.temperatureUnit || "F"}`,
        `风速：${period.windSpeed || "未知"} ${period.windDirection || ""}`,
        `${period.shortForecast || "无可用预报"}`,
        "---",
      ].join("\n"),
    );

    const forecastText = `${latitude}, ${longitude}位置预报：\n\n${formattedForecast.join("\n")}`;

    return {
      content: [
        {
          type: "text",
          text: forecastText,
        },
      ],
    };
  },
);
```
### 运行服务器

最后，实现主函数来启动服务器：

```typescript
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error("天气服务 MCP 服务器已通过标准输入输出运行");
}

main().catch((error) => {
  console.error("main() 函数发生致命错误:", error);
  process.exit(1);
});
```

请务必运行 `npm run build` 来构建你的服务器！这是确保服务器能正常连接的关键步骤。

现在让我们通过现有的 MCP 宿主程序 Claude 桌面版来测试你的服务器。

## 使用 Claude 桌面版测试服务器

<Note>

Claude 桌面版目前暂不支持 Linux 系统。Linux 用户可以继续学习[构建客户端](/quickstart/client)教程，来创建一个连接我们刚构建的服务器的 MCP 客户端。

</Note>

首先确保已安装 Claude 桌面版。[可在此处下载最新版本](https://claude.ai/download)。如果已安装，请确保更新至最新版本。

我们需要为要使用的每个 MCP 服务器配置 Claude 桌面版。为此，用文本编辑器打开位于 `~/Library/Application Support/Claude/claude_desktop_config.json` 的配置文件（若不存在请新建）。

例如，如果你已安装 [VS Code](https://code.visualstudio.com/)：

<CodeGroup>

```bash macOS/Linux
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

```powershell Windows
code $env:AppData\Claude\claude_desktop_config.json
```

</CodeGroup>

然后在 `mcpServers` 字段中添加你的服务器配置。只有当至少配置了一个服务器时，Claude 桌面版才会显示 MCP 相关界面元素。

这里我们添加一个天气服务器配置：

<CodeGroup>

```json macOS/Linux
{
  "mcpServers": {
    "weather": {
      "command": "node",
      "args": ["/绝对路径/到/父文件夹/weather/build/index.js"]
    }
  }
}
```

```json Windows
{
  "mcpServers": {
    "weather": {
      "command": "node",
      "args": ["C:\\路径\\到\\父文件夹\\weather\\build\\index.js"]
    }
  }
}
```

</CodeGroup>

这个配置告诉 Claude 桌面版：
1. 存在名为 "weather" 的 MCP 服务器
2. 通过执行 `node /绝对路径/到/父文件夹/weather/build/index.js` 命令来启动

保存文件后，重启 Claude 桌面版。

</Tab>
<Tab title='Java'>

<Note>

本快速入门基于 Spring AI MCP 自动配置和启动器。
如需手动创建同步/异步 MCP 服务器，请参阅 [Java SDK 服务器](/sdk/java/mcp-server)文档。

</Note>

现在开始构建我们的天气服务器！
[完整代码示例可在此处获取](https://github.com/spring-projects/spring-ai-examples/tree/main/model-context-protocol/weather/starter-stdio-server)

更多信息请参考 [MCP 服务器启动器](https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html)文档。
手动实现 MCP 服务器请参阅 [Java SDK 文档](/sdk/java/mcp-server)。

### MCP 服务器日志规范

实现 MCP 服务器时需特别注意日志处理：

**基于标准输入输出的服务器：**
严禁写入标准输出(stdout)，包括：
- Python 的 `print()` 语句
- JavaScript 的 `console.log()`
- Go 的 `fmt.Println()`
- 其他语言的类似 stdout 函数

写入 stdout 会破坏 JSON-RPC 消息格式导致服务器异常。

**基于 HTTP 的服务器：**
标准输出日志不受限制，因其不会干扰 HTTP 响应。

### 最佳实践
1. 使用写入 stderr 或文件的日志库
2. 确保配置的日志库不会向 STDOUT 输出
### 系统要求

- 已安装 Java 17 或更高版本
- [Spring Boot 3.3.x](https://docs.spring.io/spring-boot/installing.html) 或更高版本

### 环境配置

使用 [Spring Initializer](https://start.spring.io/) 初始化项目。

需要添加以下依赖项：

<CodeGroup>

```xml Maven
<dependencies>
      <dependency>
          <groupId>org.springframework.ai</groupId>
          <artifactId>spring-ai-starter-mcp-server</artifactId>
      </dependency>

      <dependency>
          <groupId>org.springframework</groupId>
          <artifactId>spring-web</artifactId>
      </dependency>
</dependencies>
```

```groovy Gradle
dependencies {
  implementation platform("org.springframework.ai:spring-ai-starter-mcp-server")
  implementation platform("org.springframework:spring-web")
}
```

</CodeGroup>

然后通过设置应用属性进行配置：

<CodeGroup>

```bash application.properties
spring.main.bannerMode=off
logging.pattern.console=
```

```yaml application.yml
logging:
  pattern:
    console:
spring:
  main:
    banner-mode: off
```

</CodeGroup>

[服务端配置属性](https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html#_configuration_properties)文档列出了所有可用属性。

现在让我们开始构建服务端。

## 构建服务端

### 天气服务

实现一个[WeatherService.java](https://github.com/spring-projects/spring-ai-examples/blob/main/model-context-protocol/weather/starter-stdio-server/src/main/java/org/springframework/ai/mcp/sample/server/WeatherService.java)，使用REST客户端从国家气象局API查询数据：

```java
@Service
public class WeatherService {

	private final RestClient restClient;

	public WeatherService() {
		this.restClient = RestClient.builder()
			.baseUrl("https://api.weather.gov")
			.defaultHeader("Accept", "application/geo+json")
			.defaultHeader("User-Agent", "WeatherApiClient/1.0 (your@email.com)")
			.build();
	}

  @Tool(description = "获取指定经纬度的天气预报")
  public String getWeatherForecastByLocation(
      double latitude,   // 纬度坐标
      double longitude   // 经度坐标
  ) {
      // 返回详细预报信息包括：
      // - 温度及单位
      // - 风速和风向
      // - 详细预报描述
  }

  @Tool(description = "获取美国各州天气警报")
  public String getAlerts(
      @ToolParam(description = "两字母州代码(如CA, NY)") String state
  ) {
      // 返回活动警报包括：
      // - 事件类型
      // - 受影响区域
      // - 严重程度
      // - 描述信息
      // - 安全指引
  }

  // ......
}
```

`@Service`注解会自动将服务注册到应用上下文中。
Spring AI的`@Tool`注解可以轻松创建和维护MCP工具。

自动配置会将这些工具注册到MCP服务端。

### 创建启动应用

```java
@SpringBootApplication
public class McpServerApplication {

	public static void main(String[] args) {
		SpringApplication.run(McpServerApplication.class, args);
	}

	@Bean
	public ToolCallbackProvider weatherTools(WeatherService weatherService) {
		return  MethodToolCallbackProvider.builder().toolObjects(weatherService).build();
	}
}
```

使用`MethodToolCallbackProvider`工具将`@Tools`转换为MCP服务端可执行的回调。

### 运行服务端

最后构建服务端：

```bash
./mvnw clean install
```

这将在`target`文件夹中生成`mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar`文件。

现在可以从现有MCP主机(如Claude桌面版)测试你的服务端。
## 使用Claude桌面版测试你的服务器

[!NOTE]
Claude桌面版目前暂不支持Linux系统。

首先，请确保已安装Claude桌面版。
[你可以在这里获取最新版本](https://claude.ai/download)。若已安装，请务必升级至最新版本。

我们需要为每个要使用的MCP服务器配置Claude桌面版。
打开位于`~/Library/Application Support/Claude/claude_desktop_config.json`的配置文件进行编辑。
如果文件不存在，请先创建该文件。

例如，如果你已安装[VS Code](https://code.visualstudio.com/)：

```bash macOS/Linux
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

```powershell Windows
code $env:AppData\Claude\claude_desktop_config.json
```

然后在`mcpServers`字段中添加你的服务器配置。
只有当至少配置了一个有效服务器时，Claude桌面版才会显示MCP相关界面元素。

以下是我们添加天气服务器的配置示例：

```json macOS/Linux
{
  "mcpServers": {
    "spring-ai-mcp-weather": {
      "command": "java",
      "args": [
        "-Dspring.ai.mcp.server.stdio=true",
        "-jar",
        "/绝对路径/到父目录/mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar"
      ]
    }
  }
}
```

```json Windows
{
  "mcpServers": {
    "spring-ai-mcp-weather": {
      "command": "java",
      "args": [
        "-Dspring.ai.mcp.server.transport=STDIO",
        "-jar",
        "C:\\绝对路径\\到父目录\\weather\\mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar"
      ]
    }
  }
}
```

[!NOTE]
请务必使用绝对路径指定服务器位置。

这个配置告诉Claude桌面版：
1. 存在名为"my-weather-server"的MCP服务器
2. 通过执行`java -jar /绝对路径/到父目录/mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar`命令来启动它

保存文件后，请重启Claude桌面版应用。

## 使用Java客户端测试服务器

### 手动创建MCP客户端

使用`McpClient`连接服务器：

```java
var stdioParams = ServerParameters.builder("java")
  .args("-jar", "/绝对路径/到父目录/mcp-weather-stdio-server-0.0.1-SNAPSHOT.jar")
  .build();

var stdioTransport = new StdioClientTransport(stdioParams);

var mcpClient = McpClient.sync(stdioTransport).build();

mcpClient.initialize();

ListToolsResult toolsList = mcpClient.listTools();

CallToolResult weather = mcpClient.callTool(
  new CallToolRequest("getWeatherForecastByLocation",
      Map.of("latitude", "47.6062", "longitude", "-122.3321")));

CallToolResult alert = mcpClient.callTool(
  new CallToolRequest("getAlerts", Map.of("state", "NY")));

mcpClient.closeGracefully();
```

### 使用MCP客户端Boot启动器

通过添加`spring-ai-starter-mcp-client`依赖创建启动应用：

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-mcp-client</artifactId>
</dependency>
```

并设置`spring.ai.mcp.client.stdio.servers-configuration`属性指向你的`claude_desktop_config.json`文件。
你可以复用现有的Anthropic桌面配置：

```properties
spring.ai.mcp.client.stdio.servers-configuration=file:路径/到/claude_desktop_config.json
```

启动客户端应用时，自动配置将根据claude_desktop_config.json文件创建MCP客户端实例。

更多信息请参阅[MCP客户端Boot启动器](https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-client-docs.html)参考文档。
## 更多 Java MCP 服务端示例

[starter-webflux-server](https://github.com/spring-projects/spring-ai-examples/tree/main/model-context-protocol/weather/starter-webflux-server) 展示了如何通过 SSE 传输协议创建 MCP 服务端。该项目演示了如何利用 Spring Boot 的自动配置能力来定义和注册 MCP 工具、资源及提示模板。

</Tab>

<Tab title='Kotlin'>

让我们开始构建天气查询服务！[完整代码可在此处获取](https://github.com/modelcontextprotocol/kotlin-sdk/tree/main/samples/weather-stdio-server)

### 预备知识

本快速入门指南假设您已熟悉：
- Kotlin 语言
- 大语言模型（如 Claude）

### 系统要求
- 已安装 Java 17 或更高版本

### 环境配置

首先确保已安装 `java` 和 `gradle`：
可从 [Oracle 官方 JDK 网站](https://www.oracle.com/java/technologies/downloads/) 下载 Java。通过以下命令验证安装：

```bash
java --version
```

新建项目目录并初始化：

<CodeGroup>

```bash macOS/Linux
# 创建项目目录
mkdir weather
cd weather

# 初始化 Kotlin 项目
gradle init
```

```powershell Windows
# 创建项目目录
md weather
cd weather

# 初始化 Kotlin 项目
gradle init
```

</CodeGroup>

执行 `gradle init` 后，选择以下配置：
- 项目类型：**Application**
- 编程语言：**Kotlin**
- Java 版本：**Java 17**

也可通过 [IntelliJ IDEA 项目向导](https://kotlinlang.org/docs/jvm-get-started.html) 创建项目。

添加以下依赖项：

<CodeGroup>

```kotlin build.gradle.kts
val mcpVersion = "0.4.0"
val slf4jVersion = "2.0.9"
val ktorVersion = "3.1.1"

dependencies {
    implementation("io.modelcontextprotocol:kotlin-sdk:$mcpVersion")
    implementation("org.slf4j:slf4j-nop:$slf4jVersion")
    implementation("io.ktor:ktor-client-content-negotiation:$ktorVersion")
    implementation("io.ktor:ktor-serialization-kotlinx-json:$ktorVersion")
}
```

```groovy build.gradle
def mcpVersion = '0.3.0'
def slf4jVersion = '2.0.9'
def ktorVersion = '3.1.1'

dependencies {
    implementation "io.modelcontextprotocol:kotlin-sdk:$mcpVersion"
    implementation "org.slf4j:slf4j-nop:$slf4jVersion"
    implementation "io.ktor:ktor-client-content-negotiation:$ktorVersion"
    implementation "io.ktor:ktor-serialization-kotlinx-json:$ktorVersion"
}
```

</CodeGroup>

在构建脚本中添加插件：

<CodeGroup>

```kotlin build.gradle.kts
plugins {
    kotlin("plugin.serialization") version "your_version_of_kotlin"
    id("com.github.johnrengelman.shadow") version "8.1.1"
}
```

```groovy build.gradle
plugins {
    id 'org.jetbrains.kotlin.plugin.serialization' version 'your_version_of_kotlin'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}
```

</CodeGroup>

接下来开始构建服务端程序。

## 构建服务端
### 设置实例

添加服务器初始化函数：

```kotlin
// 运行MCP服务器的主函数
fun `运行mcp服务器`() {
    // 创建带有基础实现的MCP服务器实例
    val server = Server(
        Implementation(
            name = "weather", // 工具名称为"weather"
            version = "1.0.0" // 实现版本号
        ),
        ServerOptions(
            capabilities = ServerCapabilities(tools = ServerCapabilities.Tools(listChanged = true))
        )
    )

    // 使用标准IO创建服务器通信传输层
    val transport = StdioServerTransport(
        System.`in`.asInput(),
        System.out.asSink().buffered()
    )

    runBlocking {
        server.connect(transport)
        val done = Job()
        server.onClose {
            done.complete()
        }
        done.join()
    }
}
```

### 天气API辅助函数

接下来，添加用于查询和转换国家气象局API响应的函数及数据类：

```kotlin
// 扩展函数：根据经纬度获取天气预报信息
suspend fun HttpClient.获取预报(纬度: Double, 经度: Double): List<String> {
    val points = this.get("/points/$纬度,$经度").body<Points>()
    val forecast = this.get(points.properties.forecast).body<Forecast>()
    return forecast.properties.periods.map { period ->
        """
            ${period.name}:
            温度: ${period.temperature} ${period.temperatureUnit}
            风速: ${period.windSpeed} ${period.windDirection}
            预报详情: ${period.detailedForecast}
        """.trimIndent()
    }
}

// 扩展函数：获取指定州的天气警报
suspend fun HttpClient.获取警报(州名: String): List<String> {
    val alerts = this.get("/alerts/active/area/$州名").body<Alert>()
    return alerts.features.map { feature ->
        """
            事件: ${feature.properties.event}
            区域: ${feature.properties.areaDesc}
            严重程度: ${feature.properties.severity}
            描述: ${feature.properties.description}
            应对指南: ${feature.properties.instruction}
        """.trimIndent()
    }
}

@Serializable
data class 坐标点(
    val properties: 属性
) {
    @Serializable
    data class 属性(val forecast: String)
}

@Serializable
data class 天气预报(
    val properties: 属性
) {
    @Serializable
    data class 属性(val periods: List<时段>)

    @Serializable
    data class 时段(
        val number: Int, val name: String, val startTime: String, val endTime: String,
        val isDaytime: Boolean, val temperature: Int, val temperatureUnit: String,
        val temperatureTrend: String, val probabilityOfPrecipitation: JsonObject,
        val windSpeed: String, val windDirection: String,
        val shortForecast: String, val detailedForecast: String,
    )
}

@Serializable
data class 警报(
    val features: List<特征项>
) {
    @Serializable
    data class 特征项(
        val properties: 属性
    )

    @Serializable
    data class 属性(
        val event: String, val areaDesc: String, val severity: String,
        val description: String, val instruction: String?,
    )
}
```
### 实现工具执行功能

工具执行处理器负责实际执行每个工具的逻辑。我们来添加这个功能：

```kotlin
// 创建一个带有默认请求配置和JSON内容协商的HTTP客户端
val httpClient = HttpClient {
    defaultRequest {
        url("https://api.weather.gov")
        headers {
            append("Accept", "application/geo+json")
            append("User-Agent", "WeatherApiClient/1.0")
        }
        contentType(ContentType.Application.Json)
    }
    // 安装内容协商插件用于JSON序列化/反序列化
    install(ContentNegotiation) { json(Json { ignoreUnknownKeys = true }) }
}

// 注册一个按州获取天气警报的工具
server.addTool(
    name = "get_alerts",
    description = """
        获取美国各州的天气警报。输入为两位字母的美国州代码(如CA, NY)
    """.trimIndent(),
    inputSchema = Tool.Input(
        properties = buildJsonObject {
            putJsonObject("state") {
                put("type", "string")
                put("description", "两位字母的美国州代码(如CA, NY)")
            }
        },
        required = listOf("state")
    )
) { request ->
    val state = request.arguments["state"]?.jsonPrimitive?.content
    if (state == null) {
        return@addTool CallToolResult(
            content = listOf(TextContent("必须提供'state'参数"))
        )
    }

    val alerts = httpClient.getAlerts(state)

    CallToolResult(content = alerts.map { TextContent(it) })
}

// 注册一个按经纬度获取天气预报的工具
server.addTool(
    name = "get_forecast",
    description = """
        获取特定经纬度的天气预报
    """.trimIndent(),
    inputSchema = Tool.Input(
        properties = buildJsonObject {
            putJsonObject("latitude") { put("type", "number") }
            putJsonObject("longitude") { put("type", "number") }
        },
        required = listOf("latitude", "longitude")
    )
) { request ->
    val latitude = request.arguments["latitude"]?.jsonPrimitive?.doubleOrNull
    val longitude = request.arguments["longitude"]?.jsonPrimitive?.doubleOrNull
    if (latitude == null || longitude == null) {
        return@addTool CallToolResult(
            content = listOf(TextContent("必须提供'latitude'和'longitude'参数"))
        )
    }

    val forecast = httpClient.getForecast(latitude, longitude)

    CallToolResult(content = forecast.map { TextContent(it) })
}
```

### 运行服务器

最后，实现主函数来运行服务器：

```kotlin
fun main() = `run mcp server`()
```

请确保运行`./gradlew build`来构建你的服务器。这是让你的服务器成功连接的关键步骤。

现在让我们从现有的MCP主机Claude for Desktop来测试你的服务器。
## 使用Claude桌面版测试你的服务器

<Note>

Claude桌面版目前暂不支持Linux系统。Linux用户可以继续学习[构建客户端](/quickstart/client)教程，构建一个能连接我们刚搭建的服务器的MCP客户端。

</Note>

首先，请确保已安装Claude桌面版。[可在此下载最新版本](https://claude.ai/download)。若已安装，**请务必更新至最新版**。

我们需要为Claude桌面版配置要使用的MCP服务器。操作步骤如下：
打开位于`~/Library/Application Support/Claude/claude_desktop_config.json`的配置文件（若文件不存在请新建）。

例如，若已安装[VS Code](https://code.visualstudio.com/)：

<CodeGroup>

```bash macOS/Linux
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

```powershell Windows
code $env:AppData\Claude\claude_desktop_config.json
```

</CodeGroup>

然后在`mcpServers`键下添加你的服务器配置。
只有当至少配置了一个服务器时，Claude桌面版才会显示MCP相关界面元素。

这里我们添加一个天气服务器配置示例：

<CodeGroup>

```json macOS/Linux
{
  "mcpServers": {
    "weather": {
      "command": "java",
      "args": [
        "-jar",
        "/绝对路径/父目录/weather/build/libs/weather-0.1.0-all.jar"
      ]
    }
  }
}
```

```json Windows
{
  "mcpServers": {
    "weather": {
      "command": "java",
      "args": [
        "-jar",
        "C:\\路径\\父目录\\weather\\build\\libs\\weather-0.1.0-all.jar"
      ]
    }
  }
}
```

</CodeGroup>

这个配置告诉Claude桌面版：
1. 存在名为"weather"的MCP服务器
2. 通过执行`java -jar /绝对路径/父目录/weather/build/libs/weather-0.1.0-all.jar`来启动

保存文件后，重启**Claude桌面版**应用。

</Tab>

<Tab title='C#'>

让我们开始构建天气服务器！[完整代码可在此查看](https://github.com/modelcontextprotocol/csharp-sdk/tree/main/samples/QuickstartWeatherServer)

### 预备知识

本教程假设您已熟悉：
- C#编程
- Claude等大语言模型
- .NET 8或更高版本

### MCP服务器日志注意事项

实现MCP服务器时，请特别注意日志处理：

**基于STDIO的服务器：**
切勿写入标准输出(stdout)，包括：
- Python中的`print()`
- JavaScript中的`console.log()`
- Go中的`fmt.Println()`
- 其他语言的类似输出函数

写入stdout会破坏JSON-RPC消息格式导致服务器异常。

**基于HTTP的服务器：**
标准输出日志不受影响，因为不会干扰HTTP响应。

### 最佳实践
1. 使用写入stderr或文件的日志库

### 系统要求
- 已安装[.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)或更高版本

### 环境设置

首先安装`dotnet`（如未安装），可从[微软.NET官网](https://dotnet.microsoft.com/download/)下载。验证安装：

```bash
dotnet --version
```

现在创建并设置项目：

<CodeGroup>

```bash macOS/Linux
# 新建项目目录
mkdir weather
cd weather

# 初始化C#控制台项目
dotnet new console
```

```powershell Windows
# 新建项目目录
mkdir weather
cd weather
# 初始化一个新的 C# 项目  
dotnet new console  
```  

</CodeGroup>  
运行 `dotnet new console` 后，你将获得一个新的 C# 项目。  
你可以使用你喜欢的 IDE 打开项目，例如 [Visual Studio](https://visualstudio.microsoft.com/) 或 [Rider](https://www.jetbrains.com/rider/)。  
或者，你也可以通过 [Visual Studio 项目向导](https://learn.microsoft.com/en-us/visualstudio/get-started/csharp/tutorial-console?view=vs-2022) 创建 C# 应用程序。  
项目创建完成后，添加 Model Context Protocol SDK 和托管相关的 NuGet 包：  

```bash  

# 添加 Model Context Protocol SDK 的 NuGet 包  
dotnet add package ModelContextProtocol --prerelease  

# 添加 .NET Hosting 的 NuGet 包  
dotnet add package Microsoft.Extensions.Hosting  
```  

现在，让我们开始构建你的服务器。  

## 构建你的服务器  

打开项目中的 `Program.cs` 文件，并将其内容替换为以下代码：  

```csharp  
using Microsoft.Extensions.DependencyInjection;  
using Microsoft.Extensions.Hosting;  
using ModelContextProtocol;  
using System.Net.Http.Headers;  

var builder = Host.CreateEmptyApplicationBuilder(settings: null);  

builder.Services.AddMcpServer()  
    .WithStdioServerTransport()  
    .WithToolsFromAssembly();  

builder.Services.AddSingleton(_ =>  
{  
    var client = new HttpClient() { BaseAddress = new Uri("https://api.weather.gov") };  
    client.DefaultRequestHeaders.UserAgent.Add(new ProductInfoHeaderValue("weather-tool", "1.0"));  
    return client;  
});  

var app = builder.Build();  

await app.RunAsync();  
```  

<Note>  

在创建 `ApplicationHostBuilder` 时，请确保使用 `CreateEmptyApplicationBuilder` 而不是 `CreateDefaultBuilder`。这样可以确保服务器不会向控制台输出额外的消息。此操作仅在使用 STDIO 传输的服务器中需要。  

</Note>  

这段代码设置了一个基本的控制台应用程序，使用 Model Context Protocol SDK 创建了一个支持标准 I/O 传输的 MCP 服务器。
### 气象API辅助函数

为`HttpClient`创建一个扩展类，用于简化JSON请求处理：

```csharp
using System.Text.Json;

internal static class HttpClientExt
{
    public static async Task<JsonDocument> ReadJsonDocumentAsync(this HttpClient client, string requestUri)
    {
        using var response = await client.GetAsync(requestUri);
        response.EnsureSuccessStatusCode();
        return await JsonDocument.ParseAsync(await response.Content.ReadAsStreamAsync());
    }
}
```

接下来，定义一个包含工具执行处理程序的类，用于查询和转换来自美国国家气象局API的响应：

```csharp
using ModelContextProtocol.Server;
using System.ComponentModel;
using System.Globalization;
using System.Text.Json;

namespace QuickstartWeatherServer.Tools;

[McpServerToolType]
public static class WeatherTools
{
    [McpServerTool, Description("获取美国某个州的天气警报。")]
    public static async Task<string> GetAlerts(
        HttpClient client,
        [Description("需要获取警报的美国州名。")] string state)
    {
        using var jsonDocument = await client.ReadJsonDocumentAsync($"/alerts/active/area/{state}");
        var jsonElement = jsonDocument.RootElement;
        var alerts = jsonElement.GetProperty("features").EnumerateArray();

        if (!alerts.Any())
        {
            return "该州当前无活跃警报。";
        }

        return string.Join("\n--\n", alerts.Select(alert =>
        {
            JsonElement properties = alert.GetProperty("properties");
            return $"""
                    事件: {properties.GetProperty("event").GetString()}
                    区域: {properties.GetProperty("areaDesc").GetString()}
                    严重程度: {properties.GetProperty("severity").GetString()}
                    描述: {properties.GetProperty("description").GetString()}
                    应对指南: {properties.GetProperty("instruction").GetString()}
                    """;
        }));
    }

    [McpServerTool, Description("获取指定位置的天气预报。")]
    public static async Task<string> GetForecast(
        HttpClient client,
        [Description("位置的纬度。")] double latitude,
        [Description("位置的经度。")] double longitude)
    {
        var pointUrl = string.Create(CultureInfo.InvariantCulture, $"/points/{latitude},{longitude}");
        using var jsonDocument = await client.ReadJsonDocumentAsync(pointUrl);
        var forecastUrl = jsonDocument.RootElement.GetProperty("properties").GetProperty("forecast").GetString()
            ?? throw new Exception($"接口{client.BaseAddress}points/{latitude},{longitude}未返回预报URL");

        using var forecastDocument = await client.ReadJsonDocumentAsync(forecastUrl);
        var periods = forecastDocument.RootElement.GetProperty("properties").GetProperty("periods").EnumerateArray();

        return string.Join("\n---\n", periods.Select(period => $"""
                {period.GetProperty("name").GetString()}
                温度: {period.GetProperty("temperature").GetInt32()}°F
                风速: {period.GetProperty("windSpeed").GetString()} {period.GetProperty("windDirection").GetString()}
                预报详情: {period.GetProperty("detailedForecast").GetString()}
                """));
    }
}
```

### 运行服务端

最后，使用以下命令启动服务端：

```bash
dotnet run
```

该命令将启动服务端，并通过标准输入/输出流监听传入的请求。
## 使用Claude桌面版测试你的服务器

<Note>

Claude桌面版目前暂不支持Linux系统。Linux用户可以直接进入[构建客户端](/quickstart/client)教程，构建一个能连接我们刚搭建的服务器的MCP客户端。

</Note>

首先，请确保已安装Claude桌面版。[点击此处可安装最新版本](https://claude.ai/download)。若已安装，**请务必更新至最新版本**。

我们需要为Claude桌面版配置需要使用的MCP服务器。打开位于`~/Library/Application Support/Claude/claude_desktop_config.json`的配置文件（若不存在请新建）。例如使用[VS Code](https://code.visualstudio.com/)编辑：

<CodeGroup>

```bash macOS/Linux
code ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

```powershell Windows
code $env:AppData\Claude\claude_desktop_config.json
```

</CodeGroup>

在`mcpServers`字段中添加服务器配置。只有当至少一个服务器正确配置后，Claude桌面版才会显示MCP相关界面元素。这里我们添加天气查询服务器的配置示例：

<CodeGroup>

```json macOS/Linux
{
  "mcpServers": {
    "weather": {
      "command": "dotnet",
      "args": ["run", "--project", "/ABSOLUTE/PATH/TO/PROJECT", "--no-build"]
    }
  }
}
```

```json Windows
{
  "mcpServers": {
    "weather": {
      "command": "dotnet",
      "args": [
        "run",
        "--project",
        "C:\\ABSOLUTE\\PATH\\TO\\PROJECT",
        "--no-build"
      ]
    }
  }
}
```

</CodeGroup>

这段配置告诉Claude桌面版：
1. 存在名为"weather"的MCP服务器
2. 通过执行`dotnet run /ABSOLUTE/PATH/TO/PROJECT`命令来启动它
保存文件后，请重启**Claude桌面版**。

</Tab>
</Tabs>

### 通过指令测试

现在验证Claude桌面版是否能识别我们在`weather`服务器中公开的两个工具。注意查看界面右上角的"搜索与工具"<img src="/images/claude-desktop-mcp-slider.svg" style={{display: 'inline', margin: 0, height: '1.3em'}} />图标：

<Frame>
  <img src="/images/visual-indicator-mcp-tools.png" />
</Frame>

点击滑块图标后，应该能看到列出的两个工具：

<Frame>
  <img src="/images/available-mcp-tools.png" />
</Frame>

如果Claude桌面版未能识别你的服务器，请参考[故障排除](#troubleshooting)章节进行调试。

当工具设置图标正常显示后，你可以在Claude桌面版中运行以下指令进行测试：
- 萨克拉门托的天气如何？
- 德克萨斯州有哪些生效的气象警报？

<Frame>
  <img src="/images/current-weather.png" />
</Frame>
<Frame>
  <img src="/images/weather-alerts.png" />
</Frame>

<Note>

由于数据来自美国国家气象局，查询仅支持美国境内地点。

</Note>

## 底层运行原理

当你提问时：
1. 客户端将问题发送给Claude
2. Claude分析可用工具并决定使用哪些
3. 客户端通过MCP服务器执行选定工具
4. 结果返回给Claude
5. Claude组织自然语言响应
6. 最终答案呈现给你！

## 故障排除

<AccordionGroup>
<Accordion title="Claude桌面版集成问题">

**获取Claude桌面版日志**

与MCP相关的日志文件存放在`~/Library/Logs/Claude`目录：
- `mcp.log`记录MCP连接状态及失败信息
- `mcp-server-SERVERNAME.log`文件记录对应服务器的错误日志(stderr)

可以运行以下命令查看最近日志并实时追踪新日志：

```bash
# 检查Claude日志中的错误  
tail -n 20 -f ~/Library/Logs/Claude/mcp*.log  
```  

**Claude中未显示服务器**  

1. 检查你的`claude_desktop_config.json`文件语法  
2. 确保项目路径是绝对路径而非相对路径  
3. 完全重启Claude桌面端  

**工具调用静默失败**  

如果Claude尝试使用工具但失败：  

1. 检查Claude日志中的错误  
2. 确认你的服务器能正常构建和运行  
3. 尝试重启Claude桌面端  

**以上方法均无效怎么办？**  

请参考我们的[调试指南](/legacy/tools/debugging)获取更强大的调试工具和详细指导。  

</Accordion>  
<Accordion title="天气API问题">  

**错误：无法获取网格点数据**  

通常意味着：  

1. 坐标不在美国境内  
2. NWS API出现故障  
3. 触发了速率限制  

解决方法：  

- 确认使用美国坐标  
- 在请求间添加短暂延迟  
- 查看NWS API状态页  

**错误：[州名]没有活跃警报**  

这并非错误——仅表示该州当前没有天气警报。可尝试其他州或在恶劣天气时检查。  

</Accordion>  

</AccordionGroup>  

<Note>  

如需更高级的故障排除，请参阅我们的[MCP调试指南](/legacy/tools/debugging)  

</Note>  

## 后续步骤  

<CardGroup cols={2}>  
  <Card title="构建客户端" icon="outlet" href="/quickstart/client">  
    学习如何构建能连接到你服务器的MCP客户端  
  </Card>  
  <Card title="示例服务器" icon="grid" href="/examples">  
    查看我们官方MCP服务器和实现的案例库  
  </Card>  
  <Card title="调试指南" icon="bug" href="/legacy/tools/debugging">  
    学习如何高效调试MCP服务器和集成  
  </Card>  
  <Card  
    title="使用LLM构建MCP"  
    icon="comments"  
    href="/tutorials/building-mcp-with-llms"  
  >  
    学习如何利用Claude等LLM加速MCP开发  
  </Card>  
</CardGroup>